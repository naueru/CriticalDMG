// React
import React, { Component } from 'react';

// Styles
import styles from './Register.module.scss';

class Register extends Component {
  render = () => {
    return (
      <div className={styles.registerContainer}>
        Registration is a work in progress
      </div>
    );
  };
};

export default Register;
